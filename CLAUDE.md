# Claude Code Instructions

Claude Code reads `CLAUDE.md`, not `AGENTS.md`. This repo keeps its agent instructions in
`AGENTS.md` so that every coding agent reads the same file; the import below pulls them in.

Add repo guidance to `AGENTS.md`, not here.

@AGENTS.md
