# mongodump passthrough tests use mongosync resmoke

modules:
  - name: mongo
    repo: git@github.com:10gen/mongo.git
    prefix: ${workdir}/src
    branch: v6.0
  - name: mongosync
    repo: git@github.com:10gen/mongosync
    prefix: ${workdir}/src
    branch: rcownie/REP-5481c
